@model Article[]

@foreach (var article in Model)
{
    <div class="article">
        <h3>@article.name</h3>
        <p>@article.body.Substring(0, Math.Min(article.body.Length, 100))...</p>
        <a class="btn" href="/articles/@article.id">View Details</a>
    </div>
}